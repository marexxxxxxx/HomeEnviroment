apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingressDeployment-Chart.fullname" . }}-test-connection"
  labels:
    {{- include "ingressDeployment-Chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingressDeployment-Chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
