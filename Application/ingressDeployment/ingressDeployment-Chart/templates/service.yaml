apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingressDeployment-Chart.fullname" . }}
  labels:
    {{- include "ingressDeployment-Chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingressDeployment-Chart.selectorLabels" . | nindent 4 }}
